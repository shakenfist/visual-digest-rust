# actionlint configuration for visual-digest-rust.
#
# Declares custom labels used by our self-hosted CI runners so that
# `runs-on: [self-hosted, <label>]` lines do not trigger "unknown label"
# warnings.

self-hosted-runner:
  labels:
    - vm
    - debian-12
