[package]
name = "digest-decode"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
publish = false

[[bin]]
name = "digest-decode"
path = "src/main.rs"

[dependencies]
shakenfist-visual-digest = { path = "../shakenfist-visual-digest", features = ["cli"] }
